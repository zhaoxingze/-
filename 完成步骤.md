# 模型微调技术探索课程设计完成步骤

## 1. 选题确认

本项目最终按照 `课程设计-v1.pptx` 中第四个具体选题“模型微调技术探索”完成，不再采用“新型优化算法”方向。

参考文献范围限定为用户补充的 4 篇模型微调论文：

1. `AdaptFormer: Adapting Vision Transformers for Scalable Visual Recognition`
2. `Scaling & Shifting Your Features: A New Baseline for Efficient Model Tuning`
3. `LoRA: Low-Rank Adaptation of Large Language Models`
4. `BitFit: Simple Parameter-efficient Fine-tuning for Transformer-based Masked Language-models`

## 2. 任务划分

1. 解析 PPT 第四选题要求，确定小论文需要围绕参数高效微调方法展开。
2. 提取 4 篇参考 PDF 的文本信息，整理 AdaptFormer、SSF、LoRA、BitFit 的核心思想。
3. 准备实验环境，下载 ImageNet 预训练 ResNet18 权重与 CIFAR-10 数据集。
4. 设计 CUDA 实验，比较全量微调、线性探测和多种参数高效微调方法。
5. 实现并运行实验脚本，保存逐 seed 结果、汇总结果和准确率曲线。
6. 按 `报告模板.doc` 的技术报告结构生成 Word 小论文。
7. 对 DOCX、实验结果文件和关键数值进行结构化验证。

## 3. 文献整理步骤

1. AdaptFormer 的核心思想是在预训练视觉 Transformer 中插入轻量适配器，只训练少量新增参数，使模型能够扩展到多个视觉识别任务。
2. SSF 方法使用特征缩放与平移参数调节预训练特征分布，可以在冻结大部分预训练参数的同时完成下游任务适配。
3. LoRA 假设微调增量具有低秩结构，通过训练低秩矩阵近似权重更新，减少可训练参数量。
4. BitFit 只训练偏置项和任务头，在中小规模数据下可获得接近甚至优于全量微调的表现，是一种极简的参数高效微调基线。

## 4. CUDA 实验设计

实验脚本：`F:\Exploration\src\run_finetuning_cuda_experiment.py`

实验设置：

- 数据集：CIFAR-10
- 预训练模型：ImageNet 预训练 ResNet18
- 设备：NVIDIA GeForce RTX 4060 Laptop GPU
- 训练样本：每类 100 张，共 1000 张训练图像
- 测试样本：每类 100 张，共 1000 张测试图像
- 输入尺寸：128 x 128
- 训练轮数：4 epochs
- 随机种子：2026、2027
- 混合精度：CUDA AMP

对比方法：

1. LINEAR：冻结骨干网络，只训练分类头。
2. BITFIT：训练偏置项和分类头。
3. SSF：训练特征缩放/平移参数和分类头。
4. LORA：在分类头上使用低秩增量参数。
5. ADAPTER：在特征向量后添加瓶颈适配器。
6. FULL：全量微调所有参数，作为性能和参数开销参考。

## 5. 实验执行结果

实验输出目录：`F:\Exploration\outputs\finetune`

主要结果文件：

- `finetune_summary_results.csv`
- `finetune_seed_results.csv`
- `finetune_history.csv`
- `finetune_accuracy_curves.png`
- `finetune_config.json`

汇总结果：

| 方法 | 平均测试准确率 | 标准差 | 可训练参数比例 |
|---|---:|---:|---:|
| LINEAR | 71.25% | 1.77% | 0.0459% |
| BITFIT | 77.25% | 2.19% | 0.0888% |
| SSF | 71.20% | 1.70% | 0.0550% |
| LORA | 14.25% | 4.60% | 0.0374% |
| ADAPTER | 71.30% | 0.57% | 0.6334% |
| FULL | 72.80% | 2.12% | 100.0000% |

## 6. 论文结论组织

1. BitFit 在本实验中取得最高平均准确率 77.25%，只训练 0.0888% 参数，说明偏置项在小样本迁移中具有较强适配能力。
2. 全量微调训练 100% 参数，但平均准确率为 72.80%，未超过 BitFit，说明小样本场景下全量更新更容易受数据规模和训练轮数限制。
3. Adapter 和 SSF 接近线性探测，但 Adapter 的跨 seed 标准差最低，体现出一定稳定性。
4. 本实验中的 LoRA 只作用于新初始化分类头，而不是预训练骨干层，因此缺少 LoRA 原论文中“冻结预训练权重并低秩更新预训练层”的完整条件，结果较差应作为实现局限分析。

## 7. 最终交付文件

- 小论文：`F:\Exploration\模型微调技术探索_课程设计小论文.docx`
- 完成步骤：`F:\Exploration\完成步骤.md`
- 实验脚本：`F:\Exploration\src\run_finetuning_cuda_experiment.py`
- 实验结果目录：`F:\Exploration\outputs\finetune`
