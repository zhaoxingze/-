# 模型微调技术探索课程设计完成步骤与代码运行顺序

本文档说明本项目从选题确认、文献处理、CUDA 实验到 Word 小论文生成的完整执行流程。项目最终按照 `课程设计-v1.pptx` 中第四个具体选题“模型微调技术探索”完成。

## 一、整体执行顺序

项目按照下面的顺序执行：

1. 确认选题和参考文献范围。
2. 提取 4 篇模型微调参考 PDF 的文本。
3. 准备 CIFAR-10 数据集和 ImageNet 预训练 ResNet18 权重。
4. 编写并运行 CUDA 参数高效微调实验脚本。
5. 统计实验结果并生成曲线图。
6. 根据实验结果生成课程设计小论文 DOCX。
7. 验证 DOCX 和实验产物。
8. 整理 README、步骤文档并推送到 GitHub。

这套顺序不能随意颠倒。文献提取结果用于论文相关工作部分；数据和模型权重是 CUDA 实验的前置条件；实验结果又是 Word 小论文结果表和结论分析的依据。

## 二、每一步的目的、输入、输出和解释

### 1. 确认选题

目的：明确课程设计方向，避免把不同选题混在一起。

最终选题：模型微调技术探索。

参考文献：

1. `AdaptFormer: Adapting Vision Transformers for Scalable Visual Recognition`
2. `Scaling & Shifting Your Features: A New Baseline for Efficient Model Tuning`
3. `LoRA: Low-Rank Adaptation of Large Language Models`
4. `BitFit: Simple Parameter-efficient Fine-tuning for Transformer-based Masked Language-models`

解释：这 4 篇论文都属于参数高效微调方向。AdaptFormer 代表 Adapter 类方法，SSF 代表特征缩放和平移，LoRA 代表低秩增量微调，BitFit 代表只训练偏置项的极简微调方法。

### 2. 提取参考文献文本

运行脚本：

```powershell
python F:\Exploration\tools\extract_finetune_pdfs.py
```

输入：

- 4 篇模型微调 PDF。

输出：

- `F:\Exploration\materials\finetune_pdfs\index.json`
- `F:\Exploration\materials\finetune_pdfs\*.txt`
- `F:\Exploration\materials\finetune_pdfs\*.json`

解释：PDF 文件不便于直接检索和引用，所以先提取文本。后续写论文时，相关工作部分需要根据这些文本总结 AdaptFormer、SSF、LoRA、BitFit 的核心思想。

### 3. 准备数据集和预训练权重

数据集：

- CIFAR-10
- 本地目录：`F:\Exploration\data`

预训练权重：

- ImageNet 预训练 ResNet18
- 本地目录：`F:\Exploration\models\torch\hub\checkpoints`

解释：实验脚本中 `CIFAR10(..., download=False)`，所以运行实验前必须已经准备好 CIFAR-10。ResNet18 使用 `torchvision.models.ResNet18_Weights.DEFAULT`，会读取本地缓存中的 ImageNet 权重。这样做可以保证实验真正使用预训练模型，而不是从零开始训练。

### 4. 运行 CUDA 微调实验

运行脚本：

```powershell
python F:\Exploration\src\run_finetuning_cuda_experiment.py
```

脚本位置：

- `F:\Exploration\src\run_finetuning_cuda_experiment.py`

输出目录：

- `F:\Exploration\outputs\finetune`

解释：这是整个项目最核心的实验代码。脚本开始时会检查 `torch.cuda.is_available()`，如果没有 CUDA 会直接报错，因此该实验明确是在 GPU 上执行的。

### 5. 实验内部执行顺序

`run_finetuning_cuda_experiment.py` 内部按下面顺序运行：

1. 创建实验配置 `Config`。
2. 确认 CUDA 可用，并设置设备为 `cuda`。
3. 设置实验方法列表：

```python
methods = ["linear", "bitfit", "ssf", "lora", "adapter", "full"]
```

4. 对每一种方法依次遍历两个随机种子：

```python
seeds = (2026, 2027)
```

5. 每次训练时先构建 CIFAR-10 小样本数据加载器。
6. 再构建对应方法的模型。
7. 冻结或开放相应参数。
8. 使用 AdamW 和 CUDA AMP 训练 4 个 epoch。
9. 每个 epoch 后在测试集上评估准确率和损失。
10. 保存每个 seed 的最终结果。
11. 所有方法完成后生成汇总表和准确率曲线。

所以实际训练顺序为：

1. `LINEAR seed=2026`
2. `LINEAR seed=2027`
3. `BITFIT seed=2026`
4. `BITFIT seed=2027`
5. `SSF seed=2026`
6. `SSF seed=2027`
7. `LORA seed=2026`
8. `LORA seed=2027`
9. `ADAPTER seed=2026`
10. `ADAPTER seed=2027`
11. `FULL seed=2026`
12. `FULL seed=2027`

### 6. 各实验方法的解释

#### LINEAR

只训练最后的分类头，冻结 ResNet18 骨干网络。

作用：作为最简单的迁移学习基线，用来判断预训练特征本身是否足够有效。

#### BITFIT

训练偏置项和分类头。

作用：对应 BitFit 思想，验证只开放极少量参数是否能完成下游任务适配。

#### SSF

训练特征缩放参数 `scale`、平移参数 `shift` 和分类头。

作用：对应 SSF 思想，通过调节特征分布适配下游任务。

#### LORA

在分类头上使用低秩增量矩阵。

作用：对应 LoRA 的低秩更新思想。但本实验的 LoRA 只作用于新初始化分类头，没有作用于预训练骨干层，因此结果需要作为简化实现的局限来分析。

#### ADAPTER

在 ResNet18 输出特征后添加瓶颈适配器。

作用：借鉴 AdaptFormer/Adapter 思想，用少量新增参数对特征进行残差修正。

#### FULL

训练全部 ResNet18 参数和分类头。

作用：作为全量微调参考，用来比较参数高效方法是否能以更少参数达到相近或更好的效果。

### 7. 实验结果文件说明

实验运行后生成：

- `finetune_history.csv`：每个 epoch 的测试准确率和损失。
- `finetune_seed_results.csv`：每个方法、每个随机种子的最终结果。
- `finetune_summary_results.csv`：跨 seed 汇总结果。
- `finetune_summary_results.json`：JSON 格式汇总结果。
- `finetune_config.json`：实验配置。
- `finetune_accuracy_curves.png`：六种方法的准确率曲线。

关键结果：

| 方法 | 平均测试准确率 | 标准差 | 可训练参数比例 |
|---|---:|---:|---:|
| LINEAR | 71.25% | 1.77% | 0.0459% |
| BITFIT | 77.25% | 2.19% | 0.0888% |
| SSF | 71.20% | 1.70% | 0.0550% |
| LORA | 14.25% | 4.60% | 0.0374% |
| ADAPTER | 71.30% | 0.57% | 0.6334% |
| FULL | 72.80% | 2.12% | 100.0000% |

解释：BitFit 准确率最高，同时只训练 0.0888% 参数，说明在本小样本迁移任务中，少量偏置参数和分类头已经能有效调整模型。全量微调没有超过 BitFit，说明小样本设置下更新全部参数不一定更优。

### 8. 生成 Word 小论文

运行脚本：

```powershell
python F:\Exploration\tools\build_finetune_report_docx.py
```

输入：

- `F:\Exploration\outputs\finetune\finetune_summary_results.csv`
- `F:\Exploration\outputs\finetune\finetune_seed_results.csv`
- `F:\Exploration\outputs\finetune\finetune_config.json`
- `F:\Exploration\outputs\finetune\finetune_accuracy_curves.png`

输出：

- `F:\Exploration\模型微调技术探索_课程设计小论文.docx`

解释：该脚本把实验配置、结果表、准确率曲线和文献分析写入 Word 小论文。论文结构参考 `报告模板.doc`，包括摘要、引言、研究方法、实验与结果分析、结论和参考文献。

### 9. 验证 DOCX 和结果文件

验证内容：

1. DOCX 是否是完整压缩包。
2. 论文中是否包含 `BitFit`、`LoRA`、`SSF`、`AdaptFormer`、`CUDA`、`77.25%`、`0.0888%` 等关键内容。
3. 论文是否包含表格和实验曲线图片。
4. 结果 CSV 是否记录了 CUDA 设备名称。

解释：这一步用于确认论文不是空文件，也不是只生成了格式而没有写入真实实验结论。

### 10. 整理并推送 GitHub

执行顺序：

1. 初始化 Git 仓库。
2. 添加 `.gitignore`，排除数据集、模型权重、缓存和临时文件。
3. 添加 README，说明项目内容和实验摘要。
4. 提交模型微调项目文件。
5. 合并远程仓库已有 README。
6. 推送到 `https://github.com/zhaoxingze/-.git`。

解释：`data/` 和 `models/` 没有推送，因为 CIFAR-10 压缩包和预训练权重体积较大，而且都可以重新下载。前期误选方向产生的 SAM/ESAM 文件也没有推送，以保持仓库聚焦在第四题。

## 三、代码运行先后顺序总表

如果从头复现本项目，推荐按下面顺序运行：

| 顺序 | 命令 | 作用 | 前置条件 |
|---:|---|---|---|
| 1 | `python F:\Exploration\tools\extract_finetune_pdfs.py` | 提取 4 篇参考 PDF 文本 | PDF 文件存在 |
| 2 | 准备 CIFAR-10 和 ResNet18 权重 | 提供实验数据和预训练模型 | 网络或本地缓存可用 |
| 3 | `python F:\Exploration\src\run_finetuning_cuda_experiment.py` | 在 CUDA 上运行六种微调方法 | CUDA、数据集、权重均可用 |
| 4 | `python F:\Exploration\tools\build_finetune_report_docx.py` | 根据结果生成 Word 小论文 | 第 3 步结果已生成 |
| 5 | DOCX/CSV 结构检查 | 验证论文和实验结果一致 | 第 4 步 DOCX 已生成 |
| 6 | `git add`、`git commit`、`git push` | 推送到 GitHub | 文件整理完成 |

最关键的依赖关系是：先有文献提取和 CUDA 实验，后有论文；先有实验结果 CSV 和曲线图，后运行 Word 生成脚本。

## 四、最终交付文件

- 小论文：`F:\Exploration\模型微调技术探索_课程设计小论文.docx`
- 完成步骤：`F:\Exploration\完成步骤.md`
- 实验脚本：`F:\Exploration\src\run_finetuning_cuda_experiment.py`
- 论文生成脚本：`F:\Exploration\tools\build_finetune_report_docx.py`
- 实验结果目录：`F:\Exploration\outputs\finetune`
- GitHub 仓库：`https://github.com/zhaoxingze/-`
